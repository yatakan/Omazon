#cart-show
  - if @shoppings.present?
    %p= link_to '注文画面へ進む', new_order_path
    %caption ショッピングカート
    %table
      %tr
        %th
        %th
        %th 価格
        %th 数量
      - @shoppings.each do |cart|
        %tr
          %td#cart-image= link_to image_tag("#{cart.item.image.image_url}"), item_path(cart.item)
          %td#cart-item=link_to cart.item.name,item_path(cart.item)
          %td#cart-price= cart.item.price
          %td#cart-quantity
            = form_for cart, url:"/update_item/#{cart.item_id}", method: :post do |f|
              =f.select :quantity, options_for_select(@numbers, {selected: cart.quantity.to_i} )
              =f.submit "数量の変更を確定"
            = link_to '削除', "/delete_item/#{cart.item_id}", method: :delete, data: { confirm:'カートから削除しますか?'}
    %p.price-sum 小計 #{@sum}
  - else
    %p まだカートに何も入っていません。
    %p 商品ページから「カートに入れる」を押すと、買いたい商品を選ぶことができます。
    %p カートに入れた商品が表示され、注文画面に進むことができます。
    = link_to 'トップに戻る', root_path
